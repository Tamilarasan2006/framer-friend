{
  "name": "farmers-front",
  "version": "1.0.0",
  "description": "Farmers Friend - Agricultural marketplace with server-side database",
  "main": "server.js",
  "scripts": {
    "start": "node server.js",
    "dev": "nodemon server.js",
    "mobile:hosted:check": "node scripts/check-mobile-api-base.js",
    "mobile:sync": "node scripts/sync-cordova-www.js",
    "mobile:assets": "cd cordova-app && npm run assets:android",
    "mobile:android:add": "npm run mobile:sync && cd cordova-app && npx cordova platform add android",
    "mobile:android:build": "cd cordova-app && npm run android:build",
    "mobile:android:build:hosted": "npm run mobile:hosted:check && npm run mobile:android:build",
    "mobile:android:run": "cd cordova-app && npm run android:run"
  },
  "dependencies": {
    "compression": "^1.8.1",
    "cors": "^2.8.5",
    "express": "^4.18.2",
    "mongoose": "^8.6.2"
  },
  "devDependencies": {
    "jimp": "^0.22.12",
    "nodemon": "^3.1.14",
    "pdf-parse": "^2.4.5"
  }
}
